{
  "build": {
    "php": "8.3",
    "builder": "NIXPACKS",
    "buildCommand": "composer install --no-scripts --optimize-autoloader"
  },
  "deploy": {
    "startCommand": "bash start.sh",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100
  }
}
