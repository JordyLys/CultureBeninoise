{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cp .env.example .env 2>/dev/null || true && composer install --ignore-platform-reqs --no-scripts"
  }
}
